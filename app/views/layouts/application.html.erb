<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>Creative Technologies Catalogue</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>

<body>
<div class="header-line">&nbsp;</div>
  <nav class="navbar">
    <div class="left-block">
      <div class="logo">
        <a href="/"><%= image_tag 'logo.svg', alt: 'Creative Electronic Technology' %></a>
      </div>
      <div class="nav-title">
        <p class="nav-element-text text-bold">
          <%= link_to 'Creative Electronic Technology', root_path %>
        </p>
      </div>
    </div>
    <div class="right-block">
      <div class="nav-element-text nav-right-link text-semi-bold mr-sm">
        <a href="#" id="navToggle">Навигация <i class="fa-solid fa-caret-down"></i></a>
        <ul class="dropdown-menu" id="navMenu">
          <li><a href="#catalogue">Каталог</a></li>
          <li><a href="#about">О компании</a></li>
          <li><a href="#payments">Оплата и доставка</a></li>
        </ul>
      </div>
      <div class="nav-element-text nav-right-link text-semi-bold">
        <a href="#contacts">Контакты</a>
      </div>
    </div>
  </nav>

<div class="container mt-sm">
  <% if flash[:notice].present? %>
    <p class="mb mt message message-notice"><%= flash[:notice] %></p>
  <% elsif flash[:alert].present? %>
    <p class="mb mt message message-alert"><%= flash[:alert] %></p>
  <% end %>

  <!-- Виртуальный помощник -->
  <div id="assistant" class="assistant-hidden">
    <img src="<%= asset_path('assistant/assistant-hid.png') %>" id="assistant-img" alt="Помощник" data-hid="<%= asset_path('assistant/assistant-hid.png') %>" data-start="<%= asset_path('assistant/assistant-start.png') %>"/>

    <div id="assistant-dialog">
      <p class="dialog-title">Нужна помощь?</p>
      <button onclick="handleAssistantChoice('yes')">Да, помоги мне с выбором</button>
      <button onclick="handleAssistantChoice('no')">Нет, спасибо</button>
    </div>
  </div>


  <%= yield %>

  <footer class="footer">
    <p>&copy; 2023-2025 Createhnologies. Все права защищены.</p>
  </footer>
</div>

<div class="mt-md header-line">&nbsp;</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const containers = document.querySelectorAll('.parallax-container');
    const speed     = 0.5;
    const maxOffset = 50;

    function clamp(val, min, max) {
      return Math.min(Math.max(val, min), max);
    }

    function updateParallax() {
      const windowH = window.innerHeight;

      containers.forEach(container => {
        const textBlock = container.querySelector('.parallax-text');
        const rect      = container.getBoundingClientRect();

        if (rect.bottom < 0 || rect.top > windowH) return;
        const rawMove = ( (windowH - rect.top) * speed )
          - (rect.height * speed / 2);

        const moveY = clamp(rawMove, -maxOffset, maxOffset);

        // Центрируем по X и сдвигаем по Y в пределах maxOffset
        textBlock.style.transform =
          `translateY(calc(-50% + ${moveY}px))`;
      });
    }

    window.addEventListener('scroll', updateParallax);
    window.addEventListener('resize', updateParallax);
    updateParallax();
  });
</script>
<script>
  console.log('toggle click')
  document.addEventListener('DOMContentLoaded', () => {
    const toggleLink = document.getElementById('navToggle');
    const menu       = document.getElementById('navMenu');
    const icon       = toggleLink.querySelector('i');

    toggleLink.addEventListener('click', (e) => {
      e.preventDefault();
      // переключаем видимость меню
      menu.classList.toggle('show');
      // меняем стрелку
      icon.classList.toggle('fa-chevron-down');
      icon.classList.toggle('fa-chevron-up');
    });

    // Скрывать меню при клике вне его
    document.addEventListener('click', (e) => {
      if (!toggleLink.contains(e.target) && !menu.contains(e.target)) {
        if (menu.classList.contains('show')) {
          menu.classList.remove('show');
          icon.classList.add('fa-chevron-down');
          icon.classList.remove('fa-chevron-up');
        }
      }
    });
  });
</script>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const container = document.querySelector('.star-rating');
    const labels    = Array.from(container.querySelectorAll('.star-label'));
    const inputs    = Array.from(container.querySelectorAll('.star-input'));

    labels.forEach(label => {
      label.addEventListener('click', () => {
        const val = parseInt(label.dataset.value, 10);

        inputs.forEach(input => {
          input.checked = parseInt(input.value, 10) === val;
        });

        labels.forEach(l => {
          const lval = parseInt(l.dataset.value, 10);
          if (lval <= val) l.classList.add('selected');
          else             l.classList.remove('selected');
        });

        container.classList.add('has-selection');
      });
    });
  });
</script>
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const assistant = document.getElementById("assistant");
    const assistantImg = document.getElementById("assistant-img");

    let isExpanded = false;
    const imgHid = assistantImg.dataset.hid;
    const imgStart = assistantImg.dataset.start;

    assistantImg.addEventListener("click", () => {
      isExpanded = !isExpanded;
      assistant.classList.toggle("expanded", isExpanded);
      assistant.classList.toggle("assistant-hidden", !isExpanded);
      assistantImg.src = isExpanded ? imgStart : imgHid;
    });

    window.handleAssistantChoice = function (choice) {
      if (choice === "yes") {
        alert("Отлично! Сейчас подберем товары.");
        // вставьте вызов вашего функционала
      } else {
        isExpanded = false;
        assistant.classList.remove("expanded");
        assistant.classList.add("assistant-hidden");
        assistantImg.src = imgHid;
      }
    };
  });
</script>
</body>
</html>
